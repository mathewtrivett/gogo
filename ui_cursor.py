import pygame

class UICursor():
    
    def __init__(self, board, screen, colour):
        self.coordinates = (0,0)
        
        self.pygame.Surface((screen.get_width(),screen.get_width()))
        self.image.set_colorkey(0,0,0)
